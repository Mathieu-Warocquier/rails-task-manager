<h1><%= @task.title %></h1>

<p><%= @task.details %></p>


<p><%= link_to "Back to list", tasks_path %></p>
<p><%= link_to 'Update task', edit_path(@task.id) %></p>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
